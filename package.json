{
  "name": "octopusagile.uk",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build ./index.ts --outdir=build --minify"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "apexcharts": "^4.3.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}